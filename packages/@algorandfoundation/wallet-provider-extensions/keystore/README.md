# Keystore Extension

This extension provides a keystore types that can be used with the wallet provider.
This extension may eventually be moved into the wallet provider as it's role is critical to wallet operations.